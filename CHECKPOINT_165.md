# Checkpoint 165 - Chapter 6 Finale

## Lesson

**Orthogonality and Projection: The Big Picture**

This checkpoint closes Chapter 6 by synthesizing the chapter rather than adding a new theorem.

### Card sequence

1. Perpendicularity becomes a zero dot product.
2. Projection gives an orthogonal decomposition.
3. Orthonormal coordinates simplify projection to `QQ^T`.
4. Gram-Schmidt builds `Q` and leads to `A = QR`.
5. Least squares is projection onto the column space.
6. Projection matrices and square orthogonal matrices are compared directly.
7. Four high-value recognition formulas are collected.
8. Sparse closing card: **Orthogonality turns geometry into computation.**

## Revision

`cp165_r2_center_matrix_families_heading`

## Focused verification

Run:

```zsh
zsh scripts/check_cp165_chapter_six_finale.zsh
```

Preview:

```zsh
zsh scripts/render_cp165_chapter_six_finale.zsh -pql
```


## r2 Card 6 symmetry refinement

- Repositioned “Orthogonality appears in two different matrix families” to the midpoint of the actual vertical gap between the lesson title above and the two matrix-family labels below.
- No mathematical content, timing, or other cards changed.
