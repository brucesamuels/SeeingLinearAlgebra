# CP82.3

Purpose:
- Keep the coordinate origin (red dot) fixed during the translation example.
- Introduce a separate translated-origin marker labelled `T(0)` that moves with the translated vectors and polygon.

Visual intent:
The grid and coordinate origin remain stationary while the translated objects clearly originate from `T(0)`, making `T(0) != 0` immediately visible.
