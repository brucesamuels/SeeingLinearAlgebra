# Checkpoint 97 — Scalar Multiplication of Matrices

Second lesson in the Matrix Operations chapter.

Storyboard: define `(cA)_{ij}=ca_{ij}`; animate a 2×2 example; preserve dimensions; show negative and zero scalars; introduce distributive laws; connect `(cA)x=c(Ax)`; Pause-and-Predict; bridge to CP94.

## Check
```zsh
./scripts/check_cp97_matrix_scalar_multiplication.zsh
```
## Render
```zsh
./scripts/render_cp97_matrix_scalar_multiplication.zsh
```
Do not commit until the render is approved.
