#!/bin/zsh
set -euo pipefail

repo_root="${0:A:h:h}"
cd "$repo_root"

args=("$@")
if (( ${#args[@]} == 0 )); then
  args=(-pql)
fi

python -m manim --disable_caching \
  $args \
  -o CP153_r2_left_import_hotfix_preview \
  scenes/vector_projection_presentation.py \
  VectorProjectionPresentation
